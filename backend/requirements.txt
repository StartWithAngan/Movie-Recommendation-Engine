fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0     # required for pydantic's EmailStr
motor==3.5.1               # async MongoDB driver
pymongo==4.8.0
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4     # password hashing
python-dotenv==1.0.1
python-multipart==0.0.9    # form parsing (login)

# ML stack (also used by ml/ pipeline, kept in one requirements file for
# Phase 1 simplicity; can be split into ml/requirements.txt later if the
# backend and ML environments need to diverge)
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
scipy==1.13.1
joblib==1.4.2

# Testing
pytest==8.3.2
httpx==0.27.2
